---
import Button from '../ui/Button.astro';
import Icon from '../ui/Icon.astro';
import { siteConfig } from '../../data/site.js';
import { url } from '../../utils/url.js';
---

<section class="section bg-white">
  <div class="container-w4s">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="text-h2 font-bold text-w4s-navy mb-4">
        Où intervenons-nous ?
      </h2>
      <p class="text-body-lg text-w4s-navy/70 mb-8">
        Services à domicile dans l'Oise, à Amiens et à Paris. Services digitaux dans toute la France.
      </p>

      <!-- Zones badges -->
      <div class="flex flex-wrap items-center justify-center gap-3 mb-6">
        {siteConfig.zones.map((zone) => (
          <span class="inline-flex items-center gap-1.5 px-4 py-2 bg-w4s-concrete/30 rounded-full text-w4s-navy">
            <Icon name="map-pin" size={16} class="text-w4s-accent" />
            {zone}
          </span>
        ))}
      </div>

      <!-- Digital services highlight -->
      <div class="inline-flex items-center gap-2 px-4 py-2 bg-w4s-accent/10 text-w4s-accent rounded-full font-medium mb-8">
        <span class="text-lg">🌐</span>
        Services digitaux : France entière
      </div>

      <div>
        <Button variant="ghost" href={url('/zones')}>
          Voir toutes les zones
          <Icon name="arrow-right" size={18} />
        </Button>
      </div>
    </div>
  </div>
</section>
